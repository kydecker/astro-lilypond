---
import { render } from "astro-lilypond";
import BaseLayout from "../../../shared/BaseLayout.astro";
import simple from "../scores/simple.ly";

const { Score } = await render(simple, { format: "png" });
---

<BaseLayout title="Score: png">
	<Score />
</BaseLayout>

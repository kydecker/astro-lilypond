{
	"name": "e2e",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"scripts": {
		"dev:general": "pnpm --filter astro-lilypond build && astro dev --root general",
		"dev:satteri": "pnpm --filter astro-lilypond build && astro dev --root satteri",
		"dev:unified": "pnpm --filter astro-lilypond build && astro dev --root unified",
		"test": "playwright test",
		"test:ui": "playwright test --ui",
		"test:headed": "playwright test --headed"
	},
	"dependencies": {
		"@astrojs/markdown-remark": "^7.2.1",
		"@astrojs/mdx": "^7.0.5",
		"astro": "^7.1.6",
		"astro-lilypond": "workspace:*"
	},
	"devDependencies": {
		"@playwright/test": "^1.62.1"
	}
}

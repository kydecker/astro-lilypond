---
const HTML_ENTITIES: Record<string, string> = {
	"&": "&amp;",
	"<": "&lt;",
	">": "&gt;",
	'"': "&quot;",
	"'": "&#39;",
};

function escapeHtml(text: unknown): string {
	return String(text).replace(/[&<>"']/g, (c) => HTML_ENTITIES[c] ?? c);
}

interface Props {
	content: string;
	class?: string | undefined;
	style?: string | undefined;
}

const { content, class: className, style } = Astro.props;

let html = content;
// Multi-page output nests several `<img class="lilypond">` inside an
// `<ol>` — apply class/style to every page, not just the first.
if (className)
	html = html.replace(/class="lilypond"/g, `class="lilypond ${className}"`);
if (style)
	html = html.replace(/<(svg|img)\b/g, `<$1 style="${escapeHtml(style)}"`);
---

<Fragment set:html={html} />

\version "2.26.0"

% Minimal fixture for lilypondLoader's integration test: header metadata is
% what's under test, not the music.
\header {
  title = "Sonata"
  composer = "Beethoven"
  mutopiacomposer = "BeethovenLV"
}

{ c4 }

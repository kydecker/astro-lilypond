---
title: Building and Deploying
description: Get LilyPond available in CI and other build environments.
---

import InstallLilyPond from "../../../components/InstallLilyPond.astro";

`astro build` needs a `lilypond` binary to compile your scores. The binary can be downloaded by default, or you can manually install it yourself.

## Automatic installation

When [`autoInstall`](/reference/configuration/#autoinstall) is enabled, running `astro build` will download the `lilypond` binary (if needed) and use it to compile your scores. This is helpful when your Astro site gets built by an external service like GitHub, Cloudflare, or Netlify.

:::note
If `lilypond` is already installed and available on `PATH`, it will always be used, regardless of how `autoInstall` is set.
:::

## Manual installation

### On your computer

When running `astro-lilypond` locally, you should install the LilyPond binary yourself so that you can use LilyPond in other tools.

<InstallLilyPond />

### On GitHub Actions

To manually install LilyPond using a Linux runner in GitHub Actions, run `apt-get` before your `build` step:

```yaml title=".github/workflows/deploy.yml"
- name: Install LilyPond
  run: sudo apt-get update && sudo apt-get install -y lilypond
```

:::caution
The `lilypond` apt package may lag behind the current stable release.
:::

To fail the build if `lilypond` is not found, explicitly set `autoInstall: false`:

```js
// astro.config.mjs
lilypond({
  autoInstall: false
})
```

`autoInstall: false` will prevent the integration from falling back to a different binary if the manual installation isn't found.

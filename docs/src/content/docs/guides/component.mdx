---
title: Component Usage
description: How to render LilyPond notation using the Astro component.
---

import { Code } from "@astrojs/starlight/components";
import BachInventionExample from "#components/examples/BachInventionExample.astro";
import BachInventionExampleRaw from "#components/examples/BachInventionExample.astro?raw";

Import a `.ly` or `.ily` file, call [`render()`](/reference/render/) to get a `Score`, then render it as `<Score />`.

<Code code={BachInventionExampleRaw} lang="astro" title="MyComponent.astro" />

<BachInventionExample />

When a score renders to more than one page, as above, the pages are wrapped in an ordered list element. You can style pages and containers however you want. See the [styling guide](/guides/styling/) for more examples.

:::caution
Only call `render()` from prerendered pages and components. `astro-lilypond` doesn't currently support on-demand rendering.
:::

## Cropping

By default, `render()` renders a score as a full page (or multiple pages), preserving page sizes and margins set via the imported LilyPond file.

If you'd prefer _not_ to use full pages, and instead crop all content to a single, potentially large image, pass `crop: true`:

```astro title="CroppedScore.astro" {7}
---
import { render } from "astro-lilypond";
import bachInvention from "./bach-invention.ly";

const { Score } = await render(
  bachInvention,
  { crop: true }
);
---

<Score />
```

Update your configuration to set [`defaults.crop`](/reference/configuration/#defaultscrop) to `true` if you'd like every `render()` call that omits `crop` to be cropped by default. An explicit `crop: false` always overrides that default:

```astro title="UncroppedScore.astro" {9}
---
import { render } from "astro-lilypond";
import bachInvention from "./bach-invention.ly";

// Render as multiple full pages with margins,
// even if defaults.crop is true
const { Score } = await render(
  bachInvention,
  { crop: false }
);
---

<Score />
```

## PDF download

Pass `pdf: true` to also get a downloadable PDF rendering of the same score, returned as `pdf` — LilyPond always renders it as full, uncropped pages, since a cropped/merged image isn't a sensible printable document:

```astro title="ScoreWithDownload.astro" {5-8,12}
---
import { render } from "astro-lilypond";
import bachInvention from "./bach-invention.ly";

const { Score, pdf } = await render(
  bachInvention,
  { crop: true, pdf: true }
);
---

<Score />
<a href={pdf.src} download>Download PDF</a>
```

## Limiting rendered pages

By default, `<Score />` renders every page. To show only an excerpt — the first page as a preview, for example — pass a `pageLimit`:

```astro title="PreviewScore.astro" {8}
---
import { render } from "astro-lilypond";
import bachInvention from "./bach-invention.ly";

const { Score } = await render(bachInvention);
---

<Score pageLimit={1} />
```

`pageLimit={1}` renders only the first page. `pageLimit={2}` renders the first two pages, etc. Passing a `pageLimit` larger than the number of available pages has no effect.

## Page count

Display a rendered score's page count with `pageCount`, alongside `Score`:

```astro title="ScoreWithPageCount.astro" {5,9}
---
import { render } from "astro-lilypond";
import bachInvention from "./bach-invention.ly";

const { Score, pageCount } = await render(bachInvention);
---

<Score />
<p>{pageCount} {pageCount === 1 ? "page" : "pages"}</p>
```

## Alt text

`<Score />` derives alt text automatically from the imported `.ly` file's `\header` block, displaying `alt="{title} by {composer}"`.

### Customizing

Pass an `alt` prop to override the generated alt text with your own description:

```astro title="MyComponent.astro" {8}
---
import { render } from "astro-lilypond";
import bachInvention from "./bach-invention.ly";

const { Score } = await render(bachInvention);
---

<Score alt="A piano score showing a two-part invention by Bach" />
```

See the [`render()` reference](/reference/render/) for the full list of props `<Score />` forwards, and its full set of options.
